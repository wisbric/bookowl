apiVersion: v1
kind: Service
metadata:
  name: {{ include "bookowl.fullname" . }}-api
  labels:
    {{- include "bookowl.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.api.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bookowl.apiSelectorLabels" . | nindent 4 }}
