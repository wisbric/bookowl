apiVersion: v1
kind: Service
metadata:
  name: {{ include "bookowl.fullname" . }}-web
  labels:
    {{- include "bookowl.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bookowl.webSelectorLabels" . | nindent 4 }}
